'use client';

export function HeroBackgroundSlideshow() {
  return (
    <div
      className="pointer-events-none absolute inset-0 z-0 min-h-full w-full overflow-hidden bg-white dark:bg-background"
      aria-hidden
    />
  );
}
